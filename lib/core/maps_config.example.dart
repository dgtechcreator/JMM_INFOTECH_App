/// Template — copy this file to maps_config.dart (gitignored) and fill in a real key.
/// See maps_config.dart's doc comment for what this key is used for and how to restrict it —
/// it must be a separate, unrestricted-by-app key, NOT the Android-app-restricted key from
/// android/local.properties's MAPS_API_KEY.
const String googlePlacesApiKey = 'YOUR_GOOGLE_PLACES_API_KEY';
